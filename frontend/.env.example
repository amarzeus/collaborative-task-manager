# Frontend Environment Variables
# Copy to .env.local for local development

# Backend API URL
# - For development: /api/v1 (Vite proxy handles it)
# - For production: https://your-backend.com/api/v1
VITE_API_URL=/api/v1

# Socket.io URL (optional - defaults to same as API host)
# - For development: leave empty (uses localhost:3001)
# - For production: https://your-backend.com
VITE_SOCKET_URL=
